# This should show a code sample
## Second level header
Some sentence. And here is some code example:
```javascript
const iife = ((a) => (b) => a + b)(1);
console.log(iife(2))
function x() {
  return void(0);
}
```
